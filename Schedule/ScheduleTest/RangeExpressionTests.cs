using Schedule;
using System;
using Xunit;

namespace ScheduleTest
{
    public class RangeExpressionTests
    {
        [Fact]
        public void RangeExpressionsWorkAsSpecified()
        {
            //Assert.Equal(0, new RangeExpression("0-1/1").Next(1));
            //Assert.Equal(0, new RangeExpression("0-2/1").Next(2));
            //Assert.Equal(13, new RangeExpression("10-20/3").Next(11));
            //Assert.Equal(5, new RangeExpression("5").Next(5));
            //Assert.Equal(5, new RangeExpression("5").Next(4));
            //Assert.Equal(5, new RangeExpression("5").Next(int.MinValue));
            //Assert.Equal(21, new RangeExpression("21-22/1200").Next(21));
            //Assert.Equal(0, new RangeExpression("0-999/1").Next(999));
        }
    }
}
